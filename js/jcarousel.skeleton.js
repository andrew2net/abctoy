//$(function() {
  $(function() {
    /*
     Carousel initialization
     */
    $('.jcarousel')
            .jcarousel({
      wrap: 'circular'
    }).jcarouselAutoscroll({
      interval: 5000
    });
  });
//  $('.jcarousel').jcarousel('scroll', 1);
//});
